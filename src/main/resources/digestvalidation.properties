# where the validation is done: local or webservice
validator=local
# shared keys. they can be different or exactly the same
shared_secret_incoming=something
shared_secret_response=something
# the character encoding of the digest
encoding=iso-8859-1
# the hash function. use SHA-1, SHA-256 or other members from the SHA family
hashfunction=SHA-256